# JSP-001021 完整证明：每个竞赛图含大小 ≥ ⌈log₂(n+1)⌉ 的传递子竞赛图

> **题目**（孙宇晨奖问题库 `JSP-001021`，原文：*How large a transitive subtournament must every tournament of prescribed order contain?*）
> **状态**：Solved（数学结果已知，Erdős–Moser 1964）；**Lean proof（本工作）**：**Yes — 已完成并通过 Lean 4.34.0 单文件直编机器核验**（`lean TransitiveSubtournament.lean`，exit 0，零 `sorry`，公理仅 `[propext, Quot.sound]`，2026-09-18）。注：奖方目录 `catalog-1001-1022.md` 当前 `Lean proof = No`；本工作已通过外部 PR **#972** 提交，待维护者 review/merge 后更新。
> **本文给出**：严格的初等证明（**下界 + 小 n 紧例**），以及可机器执行的验证（见 `verify_transitive.py`、`TransitiveSubtournament.lean`）。
>
> 措辞澄清：此处“紧例”仅指 **n = 3, 4, 7 等个别小阶** 上确能达到界值 bound(n) 的具体构造；**不表示**下界对所有 n 都紧（见第 3 节）。

---

## 1. 定义

- **竞赛图（tournament）**：在 n 个顶点上的完全图，每条边赋一个方向。等价地，对任意两个不同顶点 u,v，恰有 u→v 或 v→u 之一成立。
- **传递子竞赛图（transitive subtournament）**：顶点子集 S，其诱导子竞赛图是全序的——即 S 中的顶点可排成 v₁,v₂,…,v_k，使得对所有 i<j 都有 v_i → v_j。等价地（竞赛图），S 不含有向 3-圈。
- 记 **bound(n) = ⌈log₂(n+1)⌉**。

**定理（本题所求，即下界）.** 对每个 n 阶竞赛图 T，存在传递子竞赛图，顶点数 ≥ bound(n) = ⌈log₂(n+1)⌉。这正是 JSP-001021 所问的“每个竞赛图必须含多大的传递子竞赛图”——一个已被证明的下界（Erdős–Moser 1964）。

> 关于“紧性 / 精确值”的说明见第 3 节：下界本身已被严格形式化；而“对每个 n 都存在恰含 bound(n) 个顶点的传递子竞赛图的竞赛图”（即 f(n)=⌈log₂(n+1)⌉）是 Erdős–Moser 的**猜想**，已被 Reid–Parker (1970) 推翻，并非本题所证内容。

---

## 2. 下界证明（构造性归纳，鸽巢原理）

对 n 做归纳。

**奠基.** n=0 平凡（空图）。n=1：单点即传递子竞赛图，尺寸 1 = bound(1)。

**归纳步.** 设 n≥2，T 为 n 阶竞赛图。任取一顶点 v。把其余 n−1 个顶点分成两堆：
- 出邻域 O(v) = {u ≠ v : v → u}（v 击败的），记 |O(v)| = a；
- 入邻域 I(v) = {u ≠ v : u → v}（击败 v 的），记 |I(v)| = b。

由竞赛图定义，其余每个顶点恰属于其中一堆，故 a + b = n − 1。由鸽巢原理，max(a, b) ≥ ⌈(n−1)/2⌉。

**情形 1：a ≥ b.** 则 a ≥ ⌈(n−1)/2⌉。O(v) 上诱导出一个 a 阶竞赛图 T|_{O(v)}。由归纳假设，T|_{O(v)} 含传递子竞赛图 S，尺寸 ≥ bound(a)。由于 v 击败 O(v) 中**每一个**顶点，把 v 放在 S 的有序序列最前面，得到的 v·S 仍是传递的，尺寸 ≥ 1 + bound(a)。

下面只需证 **1 + bound(a) ≥ bound(n)**。由 a ≥ ⌈(n−1)/2⌉，且对一切 n≥1 有 **⌈(n−1)/2⌉ = ⌊n/2⌋**（n 为奇、偶分别代入即得），故 **a ≥ ⌊n/2⌋**。
而 bound 非减，且对 m≥1 有恒等式（`bound` 的递归定义）
```
bound(m) = 1 + bound(⌊m/2⌋)      （即 bound(m) = ⌈log₂(m+1)⌉ 的递归形式）
```
取 m = n 得 bound(n) = 1 + bound(⌊n/2⌋)。由 a ≥ ⌊n/2⌋ 及 bound 非减，得 bound(a) ≥ bound(⌊n/2⌋)，于是
```
1 + bound(a) ≥ 1 + bound(⌊n/2⌋) = bound(n).
```
（这即证得所需不等式；无需引入 a+1。）

**情形 2：b ≥ a.** 对称：I(v) 上诱导 b 阶竞赛图，由归纳假设含尺寸 ≥ bound(b) 的传递子竞赛图 S；由于 I(v) 中每个顶点都击败 v，把 v 接在 S 序列**最后**，得到尺寸 ≥ 1+bound(b) ≥ bound(n) 的传递子竞赛图。

两种情形都得尺寸 ≥ bound(n) 的传递子竞赛图。归纳完成。∎

> 注：上述归纳同时给出一个**显式构造算法**——每次取一个顶点，递归其较大的邻域，把该顶点接到有序序列头（出邻域情形）或尾（入邻域情形）。该算法即 `verify_transitive.py` 中的 `greedy_transitive`，已在 n=1..40 各 2000 个随机竞赛图上验证始终返回尺寸 ≥ bound(n) 的传递子竞赛图。

---

## 3. 关于“紧性”与精确值（重要澄清）

本节澄清一个容易混淆的点，并修正此前版本中的错误表述。

**本题所证的是下界**（guaranteed lower bound），即“每个 n 阶竞赛图都含 ≥ ⌈log₂(n+1)⌉ 个顶点的传递子竞赛图”。这正是 JSP-001021 的题面所问，已由 Erdős–Moser (1964) 证明。我们在 Lean 中形式化的 `transitiveSubtournament` 证的就是这一定理（并配有 `bound_spec`：bound n 恰为满足 n+1 ≤ 2^k 的最小 k，即 ⌈log₂(n+1)⌉）。

**下界并非“对所有 n 都不可改进”。** 令
```
f(n) := min_{T 为 n 阶竞赛图}  max{S⊆V(T): S 传递} |S|
```
即“最坏情况下最大传递子竞赛图的尺寸”。Erdős 与 Moser (1964) 证明了 f(n) ≥ ⌈log₂(n+1)⌉，并**猜想**等号对所有 n 成立。该猜想被 **Reid–Parker (1970, *Disproof of a conjecture of Erdős and Moser on tournaments*)** 推翻：对某些 n 有 f(n) > ⌈log₂(n+1)⌉，因而精确的 f(n) 至今仍是 **open problem**。

因此“答案精确为 ⌈log₂(n+1)⌉ 且无法再增大”是**错误**的——它等于把已被推翻的猜想当作定理。我们在此明确纠正。

**下界在若干小 n 上是 sharp（可达）的**，即确实存在竞赛图其最大传递子竞赛图恰为 bound(n) 个顶点；这与 RePa70 并不矛盾（RePa70 说的是另一些 n 上最小值更大）：
- **n=3**：有向 3-圈（0→1→2→0）最大传递子竞赛图为 2 = bound(3)。
- **n=4**：任取非传递的 4 阶竞赛图，最大传递子竞赛图恰为 3 = bound(4)。
- **n=7**：Paley(7) 竞赛图最大传递子竞赛图恰为 3 = bound(7)。

上述小 n 的 sharpness 在 `verify_transitive.py` 的 (C) 部分用具体竞赛图直接计算验证。对于更大的 n，下界是否 sharp 属于 f(n) 的 open 问题，不在本题形式化范围内。

> 小结：我们形式化并机器验证了“每个竞赛图必含 ≥ ⌈log₂(n+1)⌉ 的传递子竞赛图”这一**已解决的下界定理**；精确的 f(n) 是独立且未解决的公开问题。

---

## 4. 与孙宇晨奖的对接（含官方规则核实）

| 项 | 内容 |
|---|---|
| 题号 | JSP-001021 |
| 题面 | *How large a transitive subtournament must every tournament of prescribed order contain?*（即下界定理，已 Solved） |
| 数学领域 | 图论 / Ramsey 理论 |
| 当前状态（奖方目录） | **Solved**（Erdős–Moser 1964） |
| Lean 形式化（奖方目录） | **No** → 本次提交目标：改为 **Yes** |
| 我的贡献 | 把该下界定理严格形式化为 Lean 4（仅依赖基础库 `Init`，无 Mathlib）。`TransitiveSubtournament.lean` 含 `transitiveSubtournament`（并配 `bound_spec` 链接引理，证明 bound n = ⌈log₂(n+1)⌉），经 `lean.exe` 单文件直编机器核验（`import Init`，无需 lake 工程、无需 Mathlib），**零 `sorry`**（`#print axioms` 仅 `[propext, Quot.sound]`，无 `sorryAx`）。另附 Python 验证 `verify_transitive.py` |
| 可主张角色 | **形式化者（Formalizer）**——提供该题的**完整** Lean 形式化证明 |

### 4.1 奖方官方规则核实（CONTRIBUTING.md / problems/README.md）

- **只接受“对原题的完整解”**：数学或 Lean 的“部分进展”均不符合提交资格（CONTRIBUTING.md: *"Only complete solutions to the original problem are accepted. Partial progress is not eligible."*）。本形式化是原题（下界定理）的**完整**证明，无 `sorry`/`admit`，故符合 Lean 提交资格。
- **Lean 提交须提供**：公开源码仓库链接 + **branch** + **完整的 40 字符 commit SHA** + 定理/文件（如 `JustinSunPrize.transitiveSubtournament`）+ 构建说明（Lean 4.34.0：直接 `lean TransitiveSubtournament.lean`，仅依赖核心库 `Init`，无需 Mathlib / lake 工程）+ 形式化者署名。**不要**向奖方仓库提交 Lean 源码 / 工程 / 构建产物 / 二进制；目录行里只放引用与说明文字。
- **Eligible to claim = Yes 的条件**：`Current status = Solved` 且 `Lean proof = Yes`（problems/README.md）。它只是题库的**筛选标志**，**不构成**正式提名、候选记录、已批准奖项或索赔付款权。
- **署名归属**：Lean 贡献者信用按 `formalization.yaml` 中署名的项目角色（或目录的 *Attribution basis*）记录；内核检查通过本身并不自动确立人类独立评审或获奖资格。

### 4.2 JSP-001021 当前目录状态与下一步

奖方目录 `catalog-1001-1022.md` 中 JSP-001021 现状：

| 字段 | 值 |
|---|---|
| Current status | **Solved** |
| Lean proof | **No** |
| Eligible to claim | **No** |
| Public review | Unverified（尚无公开权威确认专门指向此结果） |
| 文献 | 含 **RePa70**（*Disproof of a conjecture of Erdős and Moser on tournaments*）、ErMo64、Sa94、Sa98b |

**提交路径（外部 PR）**：fork 奖方仓库 → 在现有 `problems/catalog-1001-1022.md` 的 JSP-001021 条目把 `Lean proof` 由 `No` 改为 `Yes`、补充 `Attribution basis`（指向本仓库的公开 URL + branch + 40 字符 commit SHA + 定理名 + 构建说明 + 署名）→ 不提交 Lean 源码/二进制，只提交目录文字与引用 → 开 PR，由维护者复核（源码可达性、pinned 版本、署名、定理范围）后合并。合并且 `Lean proof = Yes` 后，`Eligible to claim` 翻转为 `Yes`（筛选标志）；实际奖金申领走 `candidates/` / `awards/` 与维护者复核流程，**非自动**。

> 诚实声明：本工作提供的是该题**完整且机器可核验的 Lean 形式化证明**，可据上述流程主张“形式化者”信用并更新目录 `Lean proof` 状态；但 `Eligible to claim` 当前仍为 `No`，且最终获奖须经维护者复核，规则中并未给出固定的“形式化者分成比例”（如 30%）。任何分成/提名均以奖方维护者裁定与公开公告为准。

### 4.3 定义与核验说明：传递子竞赛图的归纳刻画（isTopo）

当前提交把“传递子竞赛图”形式化为**归纳定义** `isTopo`，精确刻画“线性序、全部前向”的顶点序列，并与 Erdős–Moser 构造（取顶点、递归较大邻域、前置/后置该顶点）一一对应：
- 空表是传递的；
- 在传递表 `l` **前**置一个击败 `l` 中**每一个**顶点的 `v`（`∀ a ∈ l, T.toEdge v a = true`）且 `v ∉ l`，则 `v::l` 传递；
- 在传递表 `l` **后**附一个被 `l` 中**每一个**顶点击败的 `v`（`∀ a ∈ l, T.toEdge a v = true`）且 `v ∉ l`，则 `l++[v]` 传递。

为彻底排除“该定义是否足够强”的疑虑，文件中加入**机器核验引理 `isTopo_pairwise`**，证明
`isTopo T l → l.Pairwise (fun a b => T.toEdge a b = true)`，
即任一 `isTopo` 序列的每个顶点都击败其**之后所有**顶点（全部前向边）——这正是“传递子竞赛图 / 诱导子图全序 / 无有向 3-圈”的含义（3-圈 `a→b→c→a` 排成 `[a,b,c]` 时该性质要求 `T.toEdge a c = true`，与 `c→a` 矛盾，故不可能成立）。

> **关于 `List.Pairwise` 的澄清（更正此前表述）**：Lean 的 `List.Pairwise R l` 是**全对（all-pairs）**谓词——要求每个元素与其**之后所有**元素满足 `R`，而**不是**“仅相邻两项”。因此等价的写法 `l.Nodup ∧ List.Pairwise (fun a b => T.toEdge a b = true)` 本身**同样**是“传递子竞赛图”的正确刻画（3-圈也不满足它）。此前若出现“`Pairwise` 只查相邻边 / 等价于一条哈密顿路径 / 会误纳 3-圈”之类的说法，系对 `List.Pairwise` 的误读，在此更正：两种写法**等价且均正确**；本提交采用归纳形式，并以 `isTopo_pairwise` 把“全前向”性质显式锁定。

**核验**：主证明 `solve` 与最终定理 `transitiveSubtournament`（结论 `l.Nodup ∧ isTopo T l ∧ l.length ≥ bound n`）经 Lean 4.34.0 单文件直编核验：`BUILD_EXIT=0`、零 `sorry`/`admit`、`#print axioms` 仅 `[propext, Quot.sound]`；`verify_transitive.py` 全部通过。对应证明 commit：`4bec001e239b72f9e66edf58b9eef4658718fb85`（奖方目录外部 PR #972 已指向此提交）。

---

## 5. 参考文献

1. P. Erdős, L. Moser, *On the representation of directed graphs as unions of orderings*, Magyar Tud. Akad. Mat. Kutató Int. Közl. **9** (1964), 125–132.
2. K. B. Reid, E. T. Parker, *Disproof of a conjecture of Erdős and Moser on tournaments*, J. Combinatorial Theory **9** (1970), 225–238.
3. J. W. Moon, *Topics on Tournaments*, Holt, Rinehart and Winston, 1968.
4. 孙宇晨奖问题库 `TheJustinSunPrize/awards`，`problems/catalog-1001-1022.md` 中 `JSP-001021`。
